name: Run Farm Receipt Processor

on:
  workflow_dispatch: # This adds the manual "Run" button to your dashboard

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository code
      uses: actions/checkout@v4

    - name: Set up Python Environment
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install Free Linux OCR Engine & Libraries
      run: |
        sudo apt-get update
        sudo apt-get install -y tesseract-ocr
        pip install opencv-python-headless numpy pytesseract pillow

    - name: Execute Automation Script
      env:
        EMAIL_USER: ${{ secrets.EMAIL_USER }}
        EMAIL_PASS: ${{ secrets.EMAIL_PASS }}
      run: python farm_receipt_processor.py

    - name: Commit and Save Finished Folders to GitHub Cloud
      run: |
        git config --global user.name "Receipt Bot"
        git config --global user.email "bot@github.com"
        git add Receipts_Processed/
        git diff --quiet && git diff --staged --quiet || (git commit -m "Auto-saved processed receipts [skip ci]" && git push)
